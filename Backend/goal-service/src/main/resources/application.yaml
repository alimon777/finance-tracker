spring:
  application:
    name: goal-service
  datasource:
    url: jdbc:mysql://localhost:3306/alimon
    username: root
    password: pass@word1
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8300